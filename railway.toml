# Railway config (config-as-code). See: https://docs.railway.com/config-as-code/reference
# Secrets (NEXTAUTH_SECRET, DATABASE_URL, etc.) must be set in Railway dashboard → Variables, not here.

[build]
# Uses default: npm ci, npm run build

[deploy]
startCommand = "npm start"
